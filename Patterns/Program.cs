using System;
using FactoryPattern;

namespace Patterns
{
    class Program
    {
        static void Main(string[] args)
        {
//            Console.WriteLine("Hello World!");
//            var factory = new Factory();
//            var RecordProcessor = factory.GetRecord(RecodeType.BankConnection);
//            Console.WriteLine($"It's time for {RecordProcessor.GetRecord()}'"); 
        }
    }
}
